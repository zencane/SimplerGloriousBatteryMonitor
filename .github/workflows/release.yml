name: Release

on:
  push:
    tags:
      - 'v[0-9]*.[0-9]*.[0-9]*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Version tag to release (e.g. v3.6.0) — must already exist'
        required: true

permissions:
  contents: write

jobs:
  release-windows:
    runs-on: windows-latest

    env:
      RELEASE_VERSION: ''   # populated in "Extract version" step

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}
          fetch-depth: 0   # full history needed for changelog generation

      - name: Extract version from tag
        id: version
        shell: pwsh
        run: |
          $tag = "${{ github.event.inputs.tag }}"
          if ([string]::IsNullOrWhiteSpace($tag)) { $tag = "${{ github.ref_name }}" }
          $version = $tag.TrimStart('v')
          echo "VERSION=$version" >> $env:GITHUB_OUTPUT
          echo "TAG=$tag" >> $env:GITHUB_OUTPUT
          echo "RELEASE_VERSION=$version" >> $env:GITHUB_ENV

      - name: Validate prerelease tag format
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.VERSION }}"

          # Reject ambiguous tags like 3.3.0-beta9 / 3.3.0-rc1.
          # Use SemVer-compatible format such as 3.3.0-beta.9 / 3.3.0-rc.1.
          if ($version -match '^[0-9]+\.[0-9]+\.[0-9]+-[A-Za-z]+[0-9]+$') {
            throw "Invalid prerelease tag '$version'. Use SemVer prerelease format like v3.3.0-beta.9"
          }

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Test
        run: dotnet test --configuration Release --no-build --verbosity normal

      - name: Generate changelog
        shell: pwsh
        run: |
          # Download git-cliff binary (latest version)
          $version = "2.12.0"
          $url = "https://github.com/orhun/git-cliff/releases/download/v${version}/git-cliff-${version}-x86_64-pc-windows-msvc.zip"
          $zipPath = "$env:TEMP\git-cliff.zip"
          $extractPath = "$env:TEMP\git-cliff"

          Invoke-WebRequest -Uri $url -OutFile $zipPath
          Expand-Archive -Path $zipPath -DestinationPath $extractPath -Force

          # Binary is in git-cliff-VERSION subdirectory
          $exePath = "$extractPath\git-cliff-${version}\git-cliff.exe"
          & $exePath --config cliff.toml --latest --strip header | Out-File -FilePath RELEASE_NOTES.md -Encoding utf8

      - name: Install Velopack CLI
        run: dotnet tool install -g vpk

      - name: Publish self-contained Windows x64
        run: >
          dotnet publish src/GBM.Desktop/GBM.Desktop.csproj
          --configuration Release
          --runtime win-x64
          --self-contained true
          --output publish/win-x64
          -p:PublishSingleFile=true
          -p:DebugType=None
          -p:DebugSymbols=false

      - name: Download previous release for delta generation
        run: >
          vpk download github
          --repoUrl https://github.com/${{ github.repository }}
          --token ${{ secrets.VELOPACK_GITHUB_TOKEN }}
          --outputDir releases/
        continue-on-error: true

      - name: Pack with Velopack
        run: >
          vpk pack
          --packId GloriousBatteryMonitor
          --packVersion ${{ steps.version.outputs.VERSION }}
          --packDir publish/win-x64
          --mainExe GBM.Desktop.exe
          --packTitle "Glorious Battery Monitor"
          --outputDir releases/
          --icon src/GBM.Desktop/Assets/app-icon.ico

      - name: Create GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.VELOPACK_GITHUB_TOKEN }}
        run: |
          $tag = "${{ steps.version.outputs.TAG }}"
          $version = "${{ steps.version.outputs.VERSION }}"
          $notes = ""
          if (Test-Path RELEASE_NOTES.md) {
            $notes = Get-Content RELEASE_NOTES.md -Raw
          }
          if ([string]::IsNullOrWhiteSpace($notes)) {
            $notes = "Release $tag"
          }

          # Create the release with gh CLI
          $assets = Get-ChildItem releases/ | ForEach-Object { $_.FullName }
          $notesFile = "RELEASE_NOTES.md"
          $notes | Out-File -FilePath $notesFile -Encoding utf8

          # SemVer pre-release detection, e.g. 1.2.3-beta.13, 1.2.3-rc.1, 1.2.3-alpha1
          $isPrerelease = $version -match '^[0-9]+\.[0-9]+\.[0-9]+-[0-9A-Za-z][0-9A-Za-z.-]*$'

          $releaseArgs = @(
            $tag
            $assets
            "--title", "Glorious Battery Monitor $tag"
            "--notes-file", $notesFile
          )

          if ($isPrerelease) {
            $releaseArgs += "--prerelease"
          }

          gh release create @releaseArgs
